# --- CONFIGURA��O DE SERVIDOR ---
server.port=8080

# --- BANCO DE DADOS LOCAL (POSTGRESQL) ---
# Se o seu banco n�o se chama 'edu_verse_db', altere a linha abaixo.
spring.datasource.url=jdbc:postgresql://localhost:5432/eduverse_db
spring.datasource.username=postgres
# ATEN��O: Coloque a senha do SEU PostgreSQL local aqui
spring.datasource.password=root

spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / HIBERNATE (MODO DEV) ---
# 'update' cria as tabelas se n�o existirem, ou altera se mudarem. Seguro para dev.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- MOCK DE VARI�VEIS DE AMBIENTE ---
# Isso � obrigat�rio para o CookieService n�o quebrar sua aplica��o localmente.
ENV_COOKIE_KEY=chave_secreta_local_para_testes_123
ENV_TEACHER_COOKIE=chave_professor_local_123

# --- SWAGGER (OPCIONAL, MAS BOM TER) ---
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html